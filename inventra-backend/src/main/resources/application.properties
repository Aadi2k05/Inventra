server.port=8081
inventra.jwt.secret=${INVENTRA_JWT_SECRET:inventra-development-secret-change-me}
inventra.jwt.expiry-seconds=${INVENTRA_JWT_EXPIRY_SECONDS:86400}
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
