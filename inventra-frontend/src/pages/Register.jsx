import React,{useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import api,{errMsg} from '../services/api';
export default function Register(){const nav=useNavigate(),[f,setF]=useState({name:'',email:'',password:''}),[busy,setBusy]=useState(false),[error,setError]=useState(''); const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await api.post('/auth/register',f);localStorage.setItem('inventra_token',r.data.token);localStorage.setItem('inventra_user',JSON.stringify({id:r.data.userId,name:r.data.name,email:r.data.email}));nav('/')}catch(e){setError(errMsg(e))}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-panel"><div className="brand auth-brand"><div className="brand-mark">I</div><div><b>Inventra</b><span>Inventory intelligence</span></div></div><div className="auth-copy"><div className="eyebrow">GET STARTED</div><h1>Create your account</h1><p>Build a smarter inventory operation from day one.</p></div><form onSubmit={submit}><ErrorBox message={error}/><label className="field"><span>Full name</span><input value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></label><label className="field"><span>Email</span><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/></label><label className="field"><span>Password</span><input type="password" minLength="6" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/></label><button className="btn primary full" disabled={busy}>{busy?'Creating…':'Create account'}</button></form><div className="auth-link">Already have an account? <Link to="/login">Sign in</Link></div></div><div className="auth-visual"><div className="visual-grid"></div><div className="visual-card"><span>AI-POWERED</span><strong>Turn stock data into action</strong><p>Forecast demand, spot anomalies and replenish intelligently.</p></div></div></div>}
function ErrorBox({message}){return message?<div className="error-box">{message}</div>:null}
